# Bobine V3.0.5 — Métriques d'Assiduité, Notation 5 Étoiles, Moteur Gapless A/B Deck & Télémétrie Matérielle Étendue

<p align="center">
  <img alt="Bobine" src="https://raw.githubusercontent.com/FantasmaGlad/Bobine/main/Assets/Images/bobine_banner.svg" width="100%">
</p>

La version **3.0.5** apporte des avancées substantielles au suivi analytique, à la fidélisation des adhérents, à la fluidité de la diffusion et à la supervision technique des bornes de diffusion Bobine. Elle introduit un **nouveau tableau de bord d'assiduité** (`/metrics`), un **système universel de notation 5 étoiles** avec jauge progressive de 5 minutes sur le pupitre coach (`/grid`) et sur l'écran grand format (`/cinema`), un **moteur vidéo Gapless A/B Deck** éliminant tout écran noir entre les cours, et une **télémétrie matérielle enrichie** mesurant la puissance en Watts, les températures, ainsi que les spécifications précises des composants (processeur, carte graphique, disque et mémoire vive avec fabricant, technologie DDR/LPDDR et fréquence maximale).

---

## Téléchargement des Binaires Officiels (Version 3.0.5)

| Plateforme & Environnement | Format | Architecture | Téléchargement Direct | Profil & Fonctionnalités Clés |
| :--- | :---: | :---: | :---: | :--- |
| ![Android](https://img.shields.io/badge/Android-3DDC84?style=flat-square&logo=android&logoColor=white) | <sub>`.apk`<br>*(Paquet natif)*</sub> | <sub>ARM64<br>*(arm64-v8a)*</sub> | <sub>[**Télécharger Bobine-3.0.5.apk**](https://github.com/FantasmaGlad/Bobine/releases/download/beta/Bobine-3.0.5.apk)</sub> | **Tablettes Tactiles & Régie Studio Mobile**<br><sub>• Régie autonome complète : remplace tout PC encombrant dans la salle</sub><br><sub>• Double affichage synchronisé : pupitre tactile (`/grid`) + grand écran HDMI (`/cinema`)</sub><br><sub>• Recueil d'avis 5 étoiles tactile à la fin de chaque séance</sub><br><sub>• Moteur vidéo Gapless fluide sans coupure d'affichage</sub> |
| ![Windows 11](https://img.shields.io/badge/Windows_11-0078D4?style=flat-square&logo=windows11&logoColor=white) | <sub>`.exe`<br>*(Installeur)*</sub> | <sub>x86-64</sub> | <sub>[**Télécharger Bobine-Setup-beta.exe**](https://github.com/FantasmaGlad/Bobine/releases/download/beta/Bobine-Setup-beta.exe)</sub> | **PC de Bureau & Ordinateurs Portables Windows**<br><sub>• Tableau de bord d'assiduité `/metrics` avec histogramme horaire 24h</sub><br><sub>• Supervision en direct de la consommation en Watts et des composants</sub><br><sub>• Pilotage par manette (Xbox/PlayStation), télécommande ou souris</sub><br><sub>• Mode capot fermé (clamshell) sans mise en veille</sub> |
| ![Debian](https://img.shields.io/badge/Debian-A81D33?style=flat-square&logo=debian&logoColor=white) ![Ubuntu](https://img.shields.io/badge/Ubuntu-E95420?style=flat-square&logo=ubuntu&logoColor=white) ![Linux Mint](https://img.shields.io/badge/Linux%20Mint-87CF3E?style=flat-square&logo=linuxmint&logoColor=white) | <sub>`.deb`<br>*(Paquet & Dépôt)*</sub> | <sub>x86-64<br>*(amd64)*</sub> | <sub>[**Dépôt APT apt.bobine.fit**](https://apt.bobine.fit)<br>[**bobine_beta_amd64.deb**](https://github.com/FantasmaGlad/Bobine/releases/download/beta/bobine_beta_amd64.deb)</sub> | **Postes Linux de Bureau & PC Reconditionnés**<br><sub>• Détection complète du matériel (CPU, GPU, RAM avec marque/type/fréquence, SSD)</sub><br><sub>• Moteur A/B Deck éliminant les écrans noirs de changement de source</sub><br><sub>• Mises à jour fluides en 1 clic via le dépôt officiel `apt.bobine.fit`</sub><br><sub>• 100% hors-ligne, conçu pour un fonctionnement continu 24/7</sub> |
| ![Apple](https://img.shields.io/badge/macOS%20Apple%20Silicon-000000?style=flat-square&logo=apple&logoColor=white) | <sub>`.dmg`<br>*(Image disque)*</sub> | <sub>Apple Silicon<br>*(arm64)*</sub> | <sub>[**Télécharger Bobine-beta.dmg**](https://github.com/FantasmaGlad/Bobine/releases/download/beta/Bobine-beta.dmg)</sub> | **MacBook, Mac mini & iMac (Apple Silicon)**<br><sub>• Décodage vidéo matériel direct via VideoToolbox sans charge CPU</sub><br><sub>• Double décodeur sans coupure lors des transitions entre cours</sub><br><sub>• Recueil des avis et statistiques d'utilisation intégrés</sub><br><sub>• Diffusion 4K silencieuse sans surchauffe ni bruit de ventilateur</sub> |
| ![Tauri](https://img.shields.io/badge/Bobine%20Assistant-FFC131?style=flat-square&logo=tauri&logoColor=black) | <sub>Binaire natif<br>*(Tauri 2)*</sub> | <sub>x86-64 (Linux)</sub> | <sub>[**Télécharger bobine-assistant**](https://github.com/FantasmaGlad/Bobine/releases/download/beta/bobine-assistant)</sub> | **Assistant Graphique de Gestion & Déploiement**<br><sub>• Scanner réseau intelligent avec résolution DNS et ports ouverts</sub><br><sub>• Déploiement et mise à jour des bornes à distance sans terminal</sub><br><sub>• Connexion SSH assistée par mot de passe ou clé SSH importée</sub> |
| ![Debian Appliance](https://img.shields.io/badge/Debian%2013-Appliance%20Kiosque-D70A53?style=flat-square&logo=debian&logoColor=white) | <sub>Script automatisé</sub> | <sub>x86-64</sub> | `curl -sSL https://bobine.fit/install.sh \| bash` | **Appliance Dédiée Headless (Mini-PC Dédié)**<br><sub>• Déploiement optimisé sur mini-PC (Dell Wyse 5070, Intel NUC, Lenovo Tiny)</sub><br><sub>• Télémétrie en temps réel des températures et de la puissance instantanée</sub><br><sub>• Redémarrage déterministe et reprise sur coupure de courant</sub> |

---

# PARTIE I — NOUVELLES FONCTIONNALITÉS & AJOUTS MAJEURS

### 1. Tableau de Bord Analytics `/metrics` & Suivi d'Assiduité des Séances
- **Suivi précis de l'assiduité (`playback_sessions`)** :
  - Démarrage automatique d'une session dès qu'une vidéo est chargée sur la sortie câblée ou réseau.
  - Taux de complétion rigoureusement calculé : une séance est déclarée menée à terme dès que la durée visionnée atteint ou dépasse **90%** de la durée totale du cours.
  - Distinction native de l'origine du lancement : manuel via la grille coach (`grid`), libre-service cinéma (`cinema`), planification automatique (`schedule`) ou borne kiosque (`kiosk`).
- **4 Indicateurs Clés de Performance (KPIs)** :
  - **Satisfaction Globale** : Moyenne sur 5 étoiles calculée sur l'ensemble des retours avec jauge de répartition par note.
  - **Taux de Complétion** : Pourcentage de séances suivies jusqu'au bout par rapport au total des séances initiées.
  - **Volume de Diffusion** : Heures totales d'activité physique dispensées sur la période.
  - **Séances du Jour** : Nombre de cours démarrés au cours des dernières 24 heures.
- **Histogramme Horaire d'Affluence sur 24 Heures** :
  - Réalisé en pur HTML/CSS natif (sans dépendance JavaScript lourde externe pour préserver les performances sur mini-PC).
  - Découpage heure par heure de 00h à 23h mettant en lumière les pics de fréquentation de la salle.
  - Identification automatique de l'**Heure de Pointe** avec badge d'accentuation dynamique.
- **Palmarès et Classement des Cours** :
  - Tableau récapitulatif classant les cours par nombre de sessions, taux de complétion effectif et note moyenne attribuée.
  - Filtres temporels (7 jours, 30 jours, mois en cours, historique complet) et filtres par canal de diffusion (Câblé / Réseau).

### 2. Système Universel de Notation 5 Étoiles (`CourseRatingWidget`)
- **Recueil d'avis à chaud** : Déclenché automatiquement à 95% de la vidéo ou lors de la fin naturelle du cours.
- **Routage contextuel d'affichage** :
  - **Mode Double Écran (`dual_screen`)** : Le widget s'affiche exclusivement sur le pupitre coach (`/grid`) dans le conteneur du cours qui vient d'être joué, évitant toute intrusion sur le grand écran cinématique (`/cinema`).
  - **Mode Autonome Headless (`headless`)** : Le widget s'affiche sur l'écran grand format (`/cinema` / sortie HDMI) avec une interface glassmorphism épurée.
- **Barre de Progression Inversée Fluide & Synchronisée** :
  - La durée du widget est calée dynamiquement sur le temps d'attente configuré entre deux séances (`wait_time_between_courses`).
  - Barre de chargement inversée (pleine au démarrage et se vidant de manière strictement continue et fluide).
  - Suppression de tout compte à rebours textuel anxiogène ou bloquant, bouton de fermeture rapide (`✕`) et touches de navigation (touches fléchées, `Escape`, manette).
  - Harmonisation complète de la couleur des étoiles actives, validées et des libellés avec la couleur d'accentuation du thème actif (`var(--accent-primary)`).

### 3. Moteur Vidéo Gapless A/B Deck (`/cinema`)
- **Problème résolu** : Sur les navigateurs Chromium et WebView, le remplacement de l'attribut `src` d'une balise vidéo entraîne la destruction puis la reconstruction du pipeline de décodage matériel GPU, provoquant un écran noir ou un flash blanc désagréable de 200 à 600 millisecondes entre deux vidéos.
- **Moteur Double Décodeur A/B Deck** :
  - Deux balises `<video>` HTML5 sont maintenues en permanence dans le DOM (`deckA` et `deckB`).
  - Transition cinématique en fondu croisé fluide de **0.4 seconde** (`crossfade`).
  - Pendant que le Deck actif termine sa lecture ou présente l'écran de fin, le Deck de réserve précharge en tâche de fond le flux vidéo suivant (`preload="auto"` et écoute de l'événement `canplay`).
  - Élimination totale des écrans noirs : la diffusion enchaîne les cours sans aucune rupture visuelle.

### 4. Découplage Modulaire des Métriques & Tiroir d'Historique Temporel (`/metrics`)
- **Architecture à Deux Espaces Ergonomiques** :
  - **Cours & Assiduité (`/metrics/courses`)** : Suivi des KPIs de séances, taux de complétion, volume d'activité en heures, histogramme horaire 24h et classement complet des cours.
  - **Système & Matériel (`/metrics/system`)** : Supervision en temps réel des ressources physiques (CPU, GPU, RAM, Stockage, Températures, Puissance instantanée, Énergie cumulée, Uptime et Runtime).
- **Tiroir Latéral d'Historique Interactif (`TelemetryHistoryDrawer.tsx`)** :
  - Clic direct sur n'importe quelle carte métrique pour déployer un tiroir animé depuis la droite.
  - Filtres temporels rapides : **1 heure**, **6 heures**, **24 heures** et **7 jours**.
  - Graphiques SVG responsifs dynamiques calculant automatiquement les extrêmes (minimum, maximum) et la moyenne sur la période sélectionnée.
- **Base de Données Extensible EAV (`SystemMetricLog`)** :
  - Table SQLite `system_metrics_logs` conçue pour accueillir de manière évolutive toute nouvelle sonde matérielle future sans nécessiter de migration lourde.
- **Rétention Configurable & Purge Automatique (`logs_retention_days`)** :
  - Paramétrage direct du délai de conservation des logs et de la télémétrie dans Réglages (`GET`/`PUT /api/settings`, défaut : 7 jours).
  - Tâche de fond automatique orchestrée par `scheduler_manager.py` purgeant les données antérieures au seuil configuré.

### 5. Télémétrie Matérielle & Supervision Énergétique Universelle Multi-OS
- **Détection des Composants Commerciaux Réels Multi-OS** :
  - **Android (Tablettes & Appareils Mobiles)** : Détection exacte du processeur et SoC (`Qualcomm Snapdragon 8 Elite`, `Qualcomm Snapdragon 8 Gen 3`, `MediaTek Dimensity`...), du processeur graphique (`Qualcomm Adreno 825`), de la mémoire vive (`LPDDR5X 8533 MT/s`), du stockage (`Flash UFS 4.0 (256 Go)`), des températures CPU/GPU et de la charge instantanée.
  - **Linux Bureau & Appliance Headless (Wyse 5070 / Mini-PC)** : Identification exhaustive du processeur (`AMD Ryzen 7 8840U`, `Intel Celeron J4105`), de la carte graphique (`AMD Radeon 780M`, `Intel UHD Graphics 600`, `NVIDIA GeForce`), de la mémoire vive (fabricant réel `Samsung`, `Hynix`, `Crucial`, `Micron`, génération `DDR4`/`DDR5`, fréquence maximale en MHz) et du modèle exact de SSD/NVMe (`SAMSUNG MZVL...`, `SanDisk DF...`).
  - **macOS (Apple Silicon & Intel)** : Marque de processeur via `sysctl`, chipset graphique Apple Silicon (`Apple M1 Pro`, `M2`, `M3`), mémoire vive unifiée et disque SSD.
  - **Windows** : Détection via registre Windows et interfaces WMI/PowerShell sans latence.
- **Supervision Énergétique Universelle Multi-Facteur & Multi-OS** :
  - **Calcul physique sur secteur et sur batterie** :
    - *PC Fixe Windows (Tour ATX)* : Détection de l'absence de batterie et modélisation réaliste de la consommation de bureau (28W à 110W selon charge).
    - *Mac mini / Mac Studio* : Modélisation Apple Silicon ultra-efficace sans batterie (6.5W base, 7W à 35W).
    - *MacBook branché ou sur batterie* : Interrogation native `ioreg` (charge active en direct `P = U * I`, maintien secteur à pleine charge, ou décharge sur batterie).
    - *Laptops Windows* : Utilisation instantanée de l'API Win32 `GetSystemPowerStatus` via `psutil` (charge active, maintien secteur ou décharge batterie).
    - *Headless Linux (Wyse 5070)* : Lecture différentielle micro-joule par micro-joule via Intel RAPL (`/sys/class/powercap/intel-rapl/`), avec repli mini-PC Celeron (4.8W idle, ~15W en charge).
    - *Linux Laptop* : Capteurs matériels APU/GPU `amdgpu` priorisés sur les sondes de fuite batterie `BAT1`, et détection AC `online`.
    - *Android (Xiaomi Pad 8)* : Gestion `BatteryManager` AOSP en charge et maintien secteur à 100% (3.7W validé en direct).
  - **Énergie Cumulée & Puissance Moyenne** : Intégration trapézoïdale continue de la puissance instantanée au fil du temps (`cumulative_energy_wh = ∫ P dt`), exposée en Watt-heures ($Wh$) aux côtés de la puissance moyenne ($W$).
  - **Températures et Temps d'Activité** : Températures réelles CPU/GPU ($^\circ\text{C}$), Uptime système d'exploitation et Runtime applicatif Bobine continu.

---

# PARTIE II — BUGS CORRIGÉS & FIABILISATION SYSTÈME

### 1. Résolution du Crash de la Page `/metrics`
- **Cause racine** : L'accès direct à `c.total_duration_hours.toFixed(1)` déclenchait une exception non rattrapée lorsque le point d'accès ne renvoyait que `duration_seconds` et `sessions_count`, activant l'écran d'erreur Next.js (*"This page couldn't load"*). De plus, le calcul d'échelle horaire `Math.max(1, ...hourly.map(h => h.sessions))` pouvait produire `NaN` sur des valeurs manquantes.
- **Correctif** : Calcul et exposition serveur de `total_duration_hours`, `total_sessions` et `completed_sessions` dans `course_stats`, sécurisation défensive de tous les accès numériques dans le composant React et protection de l'histogramme 24h.

### 2. Correction du Délai de Rétention des Journaux (HTTP 500)
- **Cause racine** : `routers/settings.py` acceptait `logs_retention_days` dans les champs numériques modifiables, mais l'instance Pydantic `runtime_settings` ne déclarait pas ce champ, provoquant une exception `ValueError` lors du `setattr`.
- **Correctif** : Déclaration de `logs_retention_days: int = 7` sur le schéma `Settings`, validation stricte (`>= 1` jour, rejet HTTP 400 explicite en cas de valeur invalide), et purge automatique programmée dans `scheduler_manager.py`.

### 3. Fiabilisation du Calcul de Consommation sur Secteur
- **Cause racine** :
  - Sur PC portable Linux branché sur secteur, la sonde batterie `BAT1` renvoyait 0.05 W (courant de fuite) et écrasait le capteur matériel GPU `amdgpu` (31 W).
  - Sur tablette Android à 100% sur chargeur, le courant chimique aux bornes de la batterie tombait à 0 µA, ce qui était rejeté par `if current_ua != 0:`, masquant le calcul et provoquant un `NameError` masqué par try/except.
- **Correctif** : Priorisation des capteurs APU/GPU/RAPL, détection de l'état branché (`plugged > 0`), calcul de la consommation active du SoC et de l'écran, et correction de l'appel de fonction `get_cpu_percent()`.

### 4. Harmonisation Chromatique Complète WCAG 2.1 AA/AAA
- **Amélioration UI/UX** : Élimination des icônes d'éclair et unités de puissance codées en dur en jaune/ambre (`--accent-warning`), et des étoiles codées en dur.
- L'ensemble des éléments de satisfaction, jauges et indicateurs d'alimentation adoptent dynamiquement la variable CSS du thème actif (`var(--accent-primary)`).

### 5. Nettoyage de l'Interface des Paramètres et de la Bibliothèque
- **Affichage Flash UFS compact** : Condensation du libellé "Flash UFS 4.0 (256 Go)" pour tenir de façon épurée sur une seule ligne.
- **Bibliothèque épurée** : Suppression de la barre d'outils supérieure superflue au-dessus du panneau « Métadonnées du cours ».

### 6. Synchronisation et Alignement des Versions Applicatives
- **Cause racine** : Chaquopy sur Android ne lisait pas le fichier `VERSION` situé à la racine du projet et se rabattait sur une constante périmée, affichant `V3.0.4`.
- **Correctif** : Copie automatisée de `VERSION` et `COMMIT` lors du staging des sources Python (`stagePythonSources`), passage explicite de `BuildConfig.VERSION_NAME` au démarrage du service d'arrière-plan Android, et repli par défaut sur `3.0.5`.

### 7. Clôture Déterministe des Sessions d'Assiduité
- **Cause racine** : Lors d'un arrêt anticipé par le coach ou d'une perte soudaine de connexion WebSocket, la durée visionnée de la session pouvait rester indéterminée.
- **Correctif** : Interception centralisée dans `PlaybackManager.stop()`, `PlaybackManager.video_ended()` et dans les routes de lecture pour clore systématiquement la session en cours, enregistrer la durée finale exacte et calculer le ratio d'assiduité.

### 8. Télémétrie RAM Universelle sans Privilèges Root
- **Cause racine** : Les commandes traditionnelles telles que `dmidecode` exigent les privilèges administrateur (root) sous Linux, échouant silencieusement lorsqu'elles sont interrogées par le démon non privilégié.
- **Correctif** : Interrogation prioritaire des tables SMBIOS exposées par `udevadm info -p /devices/virtual/dmi/id`, accessible en lecture à tout utilisateur standard, avec replis successifs sur `inxi`, `journalctl`, `sysfs`, WMI (Windows) et `system_profiler` (macOS).

### 9. Renforcement de la Cohérence Temporelle & Accessibilité
- **Correctif** : Protection anti-rebond préservée à 1,5 seconde sur les clics de grille, lissage strictement monotone du temps de lecture, et intégration des métadonnées de notation sans impact sur les performances de la boucle de lecture.
- **Accessibilité** : Respect strict des ratios de contraste WCAG 2.1 AA/AAA sur l'ensemble des 16 thèmes de l'application, sans aucun emoji textuel.
